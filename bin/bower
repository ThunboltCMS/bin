#!/usr/bin/env php
<?php

use Thunbolt\Console\Console;

require __DIR__ . '/../src/autoload.php';

if (Console::existScript('bower')) {
	$executor = 'bower';
} else {
	if (!file_exists($executor = __DIR__ . '/../node_modules/.bin/bower')) {
		Console::error('Cannot run bower. Not installed.');
	}
}

chdir(__DIR__ . '/..');
system($executor . ' install');
